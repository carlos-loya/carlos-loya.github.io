# carlos-loya.github.io

Personal portfolio site. Built with Vite + React + TypeScript + Tailwind CSS.

## Development

```bash
npm install
npm run dev      # start dev server at http://localhost:5173
npm run build    # production build to dist/
```
